DATABASE_URL=postgresql://usuario:senha@localhost:5432/texflow
PORT=3333
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name
# Chave usada para assinar/validar os tokens JWT de login (Etapa 7). Qualquer
# string longa e aleatória serve; troque por um valor único em produção.
JWT_SECRET=troque-por-uma-string-aleatoria-longa
